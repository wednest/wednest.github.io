"use strict";

var prevScrollPos = window.pageYOffset;

window.onscroll = function () {
  var currentScrollPos = window.pageYOffset;

  if (prevScrollPos > currentScrollPos) {
    document.querySelector('.js_top-nav').classList.remove('hidden');
  } else {
    document.querySelector('.js_top-nav').classList.add('hidden');
  }

  prevScrollPos = currentScrollPos;
};

$('.js_latest-projects__slider').slick({
  arrows: false,
  dots: true,
  dotsClass: 'latest-projects__slider__dots',
  slidesToShow: 4,
  slidesToScroll: 4
});
$('.js_latest-news__slider').slick({
  arrows: false,
  dotsClass: 'latest-projects__slider__dots',
  slidesToShow: 3,
  slidesToScroll: 3
});
$('.js_clients__slider').slick({
  slidesToShow: 2,
  slidesToScroll: 2,
  appendArrows: '.clients__slider__arrows',
  prevArrow: '<div class="clients__slider__arrows__item prev"></div>',
  nextArrow: '<div class="clients__slider__arrows__item next"></div>'
});